import React from 'react';
import ReactDOM from 'react-dom';
import './style/main.css'; // Make sure this exists or remove this line if you do not have an index.css
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
